@model WebApplication6.Models.GasN

@{
    ViewBag.Title = "站名";
}

@*@if (ViewBag.message != null)
{
    <script type="text/javascript">
        var message = @Html.Raw(Json.Encode(ViewBag.message));
        alert(message);
    </script>
}*@
@*<h2>GName</h2>*@
@using (Html.BeginForm(FormMethod.Post))
{
    @Html.AntiForgeryToken()

<div class="form-horizontal">
    <h4>新增站名</h4>
    <hr />
    @Html.ValidationSummary(true, "", new { @class = "text-danger" })
    <h4 style="color:red;">@ViewBag.Message</h4>
    <div class="form-group">
        @Html.LabelFor(model => model.Name, htmlAttributes: new { @class = "control-label col-md-2" })
        <div class="col-md-10">
            @Html.EditorFor(model => model.Name, new { htmlAttributes = new { @class = "form-control" } })
            @Html.ValidationMessageFor(model => model.Name, "", new { @class = "text-danger" })

        </div>
    </div>

    <div class="form-group">
        <div class="col-md-offset-2 col-md-10">
            <input type="submit" value="新增站名" class="btn btn-default" />
        </div>
    </div>
</div>
}
<table border="1" style="font-size:20px">

    @if (!ViewBag.Data.Count.Equals(0))
    {
        int i = 1;
        foreach (var item in ViewBag.Data)
        {

            <tr>
                <td> @i</td>
                <td> @item.Name</td>
                <td>@Html.ActionLink("刪除紀錄", "GNameDelete", new { Name = item.Name })</td>
            </tr>
            i++;

        }
    }
    else
    {
        <tr>
            <td colspan="1">
                資料庫內查無資料
            </td>


        </tr>
    }
</table>

<div>
    @Html.ActionLink("返回列表", "Index")
</div>
@section Scripts
{
    @Scripts.Render("~/bundles/jqueryval")
}